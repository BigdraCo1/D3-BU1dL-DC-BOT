// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  discordId String @id
  walletEvmId  String?    @unique
  walletSvmId  String?    @unique
  walletSuiId  String?    @unique
  walletEvm WalletEVM? @relation(fields: [walletEvmId], references: [address])
  walletSvm WalletSVM? @relation(fields: [walletSvmId], references: [address])
  walletSui WalletSUI? @relation(fields: [walletSuiId], references: [address])
}

model WalletEVM {
  address   String   @id
  user      User     @relation(fields: [userId], references: [discordId])
  userId    String   @unique
}

model WalletSVM {
  address   String   @id
  user      User     @relation(fields: [userId], references: [discordId])
  userId    String   @unique
}

model WalletSUI {
  address   String   @id
  user      User     @relation(fields: [userId], references: [discordId])
  userId    String   @unique
}
